# A sample Guardfile
# More info at https://github.com/guard/guard#readme

group :development do
  guard 'sass', :input => 'stylesheets'

  guard :livereload do
    watch(%r{.+\.(css|js|html?)$})
  end

end
